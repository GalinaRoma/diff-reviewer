/**
 * The enum of the row types.
 */
export enum RowType {
  /**
   * The row with an old version of text.
   */
  oldRow = 'oldRow',
  /**
   * The row with an new version of text.
   */
  newRow = 'newRow',
}
